# Declaracion de variable string
titulo = "Curso de Python 3"

# Ciclo for usando break
for caracter in titulo:
  # Condicional if  
  if caracter == "P":
    break
  print(caracter)


  # Ciclo for usando continue
for caracter in titulo:
  # Condicional if  
  if caracter == "P":
    continue
  print(caracter)